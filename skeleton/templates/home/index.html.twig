{% extends 'base.html.twig' %}

{% block title %}XtremShoppingManagerDeluxe{% endblock %}

{% block content %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    
</style>

<div class="container">
    <h1 class="text-center page-title">Vos listes</h1>
    <div class="listesContainer">
    {% for liste in listes %}
        <a href=" {{ path('liste_show', {'id': liste.id}) }} " class="list-link">
            <div class="paper">
            <div class="pin">
            </div>
            <div class="headerListeWrapper">
                <h2>{{ liste.titre }}</h2>
                <p>{{liste.contient.count}}</p>
            </div>
            </div>
                            {# <div class="listeWrapper">
                                    <div class="headerListeWrapper">
                                        <h2>{{ liste.titre }}</h2>
                                        <p>{{liste.contient.count}}</p>
                                    </div>
                                    <div>
                                        <ul>
                                            {% for contient in liste.contient %}
                                                {% for propose in contient.propose %}
                                                    <li>{{propose.nom}}</li>
                                                {% endfor %}
                                            {% endfor %}
                                            
                                        </ul>
                                    </div>
                                </div> #}
        </a>
    {% endfor %}
        <a href=" {{ path('liste_new') }} " class="list-link">
            <div class="paper">
            <div class="pin">
            </div>
            <div class="headerListeWrapper">
                <h2> + Nouvelle liste</h2>
            </div>
        </div>
    </div>
    <br />

</div>



{% endblock %}
